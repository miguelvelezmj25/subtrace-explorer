[
  "(MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE) || (!MAX_BUFFERED_DOCS && ((RAM_BUFFER_SIZE_MB && (!USE_COMPOUND_FILE || !COMMIT_ON_CLOSE)) || (!RAM_BUFFER_SIZE_MB && !USE_COMPOUND_FILE))))",
  "(MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE)",
  "!READER_POOLING",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && !USE_COMPOUND_FILE)",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !USE_COMPOUND_FILE)",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE) || (RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS))",
  "READER_POOLING",
  "(!USE_COMPOUND_FILE && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && !COMMIT_ON_CLOSE) || (!MAX_BUFFERED_DOCS && ((RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE) || (!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB))))",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE)",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB)",
  "(CHECK_PENDING_FLUSH_UPDATE && !COMMIT_ON_CLOSE)",
  "(RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE)",
  "(CHECK_PENDING_FLUSH_UPDATE && !MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE)",
  "((MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS) || (MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS))",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE)",
  "(CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE)",
  "(!CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && (RAM_BUFFER_SIZE_MB || !MAX_TOKEN_LENGTH))",
  "CODEC",
  "(!USE_COMPOUND_FILE && MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH)",
  "!RAM_PER_THREAD_HARD_LIMIT",
  "(!MAX_BUFFERED_DOCS && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(MAX_BUFFERED_DOCS && !MAX_TOKEN_LENGTH)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(USE_COMPOUND_FILE && !COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS && USE_COMPOUND_FILE)",
  "RAM_PER_THREAD_HARD_LIMIT",
  "(!USE_COMPOUND_FILE && (!MAX_TOKEN_LENGTH || !COMMIT_ON_CLOSE) && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH && USE_COMPOUND_FILE)",
  "(RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(USE_COMPOUND_FILE && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "!MERGE_POLICY",
  "(!RAM_BUFFER_SIZE_MB && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(!USE_COMPOUND_FILE && !COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(USE_COMPOUND_FILE && !MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(!USE_COMPOUND_FILE && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH || !COMMIT_ON_CLOSE || RAM_BUFFER_SIZE_MB || MAX_BUFFERED_DOCS)",
  "(!RAM_BUFFER_SIZE_MB && !USE_COMPOUND_FILE && !MAX_BUFFERED_DOCS)",
  "(MAX_BUFFERED_DOCS && (!MAX_TOKEN_LENGTH || !COMMIT_ON_CLOSE))",
  "(USE_COMPOUND_FILE && MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "!CODEC",
  "(RAM_BUFFER_SIZE_MB && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !USE_COMPOUND_FILE)",
  "MERGE_SCHEDULER",
  "(RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE)",
  "(USE_COMPOUND_FILE && (!MAX_TOKEN_LENGTH || !COMMIT_ON_CLOSE) && MAX_BUFFERED_DOCS)",
  "(MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)",
  "!SIMILARITY",
  "(MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)",
  "(MAX_TOKEN_LENGTH && !RAM_PER_THREAD_HARD_LIMIT)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB) || (!MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB))",
  "(RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && USE_COMPOUND_FILE)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "!NO_CFS_RATIO",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS)",
  "!CHECK_PENDING_FLUSH_UPDATE",
  "RAM_BUFFER_SIZE_MB",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(!RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE)",
  "((MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE) || (!MAX_BUFFERED_DOCS && (RAM_BUFFER_SIZE_MB || (!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE))))",
  "(!MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && !USE_COMPOUND_FILE && !COMMIT_ON_CLOSE)",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE)",
  "((MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH) || (RAM_BUFFER_SIZE_MB && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS))",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && USE_COMPOUND_FILE)",
  "COMMIT_ON_CLOSE",
  "(!MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH && RAM_PER_THREAD_HARD_LIMIT)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH)",
  "(!MAX_TOKEN_LENGTH && !RAM_PER_THREAD_HARD_LIMIT)",
  "((MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE) || (!MAX_BUFFERED_DOCS && (!USE_COMPOUND_FILE || !MAX_TOKEN_LENGTH || RAM_BUFFER_SIZE_MB)))",
  "(CHECK_PENDING_FLUSH_UPDATE && !MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB)",
  "(!MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH && !USE_COMPOUND_FILE)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "MERGE_POLICY",
  "(MAX_TOKEN_LENGTH && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE)",
  "!USE_COMPOUND_FILE",
  "!MAX_TOKEN_LENGTH",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE)",
  "(!MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)",
  "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)",
  "(!RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS)",
  "CHECK_PENDING_FLUSH_UPDATE",
  "(!RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE) || (!MAX_BUFFERED_DOCS && (RAM_BUFFER_SIZE_MB || !USE_COMPOUND_FILE)))",
  "(!RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && USE_COMPOUND_FILE && !COMMIT_ON_CLOSE)",
  "(RAM_BUFFER_SIZE_MB && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && ((RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE) || (!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB)))",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "MAX_BUFFERED_DOCS",
  "(!USE_COMPOUND_FILE && !MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "!RAM_BUFFER_SIZE_MB",
  "(!RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB) || (RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS))",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && USE_COMPOUND_FILE)",
  "(RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && ((RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE) || (!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB)))",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !USE_COMPOUND_FILE && !COMMIT_ON_CLOSE)",
  "(CHECK_PENDING_FLUSH_UPDATE && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && RAM_PER_THREAD_HARD_LIMIT)",
  "(MAX_BUFFERED_DOCS && !COMMIT_ON_CLOSE)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "(RAM_BUFFER_SIZE_MB || COMMIT_ON_CLOSE || MAX_BUFFERED_DOCS)",
  "(!MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && USE_COMPOUND_FILE)",
  "MAX_CFS_SEGMENT_SIZE_MB",
  "!MERGE_SCHEDULER",
  "(MAX_BUFFERED_DOCS && USE_COMPOUND_FILE)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(CHECK_PENDING_FLUSH_UPDATE && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH)",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "!MAX_BUFFERED_DOCS",
  "(CHECK_PENDING_FLUSH_UPDATE && !MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH)",
  "(!CHECK_PENDING_FLUSH_UPDATE && !COMMIT_ON_CLOSE)",
  "(!RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "USE_COMPOUND_FILE",
  "(RAM_BUFFER_SIZE_MB && (!MAX_TOKEN_LENGTH || !COMMIT_ON_CLOSE) && !MAX_BUFFERED_DOCS)",
  "MAX_TOKEN_LENGTH",
  "!MAX_CFS_SEGMENT_SIZE_MB",
  "(RAM_BUFFER_SIZE_MB || !MAX_TOKEN_LENGTH || MAX_BUFFERED_DOCS)",
  "(!RAM_BUFFER_SIZE_MB && USE_COMPOUND_FILE && !MAX_BUFFERED_DOCS)",
  "(MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(USE_COMPOUND_FILE && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(!RAM_BUFFER_SIZE_MB && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && !MAX_TOKEN_LENGTH) || (RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS))",
  "(MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE)",
  "(COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !USE_COMPOUND_FILE)",
  "(MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS && USE_COMPOUND_FILE)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)",
  "(COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && USE_COMPOUND_FILE)",
  "(RAM_BUFFER_SIZE_MB || (!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE) || MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "((MAX_BUFFERED_DOCS && COMMIT_ON_CLOSE) || (!MAX_BUFFERED_DOCS && ((RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE) || (!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && !RAM_BUFFER_SIZE_MB))))",
  "NO_CFS_RATIO",
  "((MAX_BUFFERED_DOCS && !COMMIT_ON_CLOSE) || (RAM_BUFFER_SIZE_MB && !COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS))",
  "!COMMIT_ON_CLOSE",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE)",
  "(CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && COMMIT_ON_CLOSE && MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE && !USE_COMPOUND_FILE && MAX_BUFFERED_DOCS)",
  "(!USE_COMPOUND_FILE || !MAX_TOKEN_LENGTH || RAM_BUFFER_SIZE_MB || MAX_BUFFERED_DOCS)",
  "(!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE && RAM_BUFFER_SIZE_MB && MAX_BUFFERED_DOCS)",
  "(!CHECK_PENDING_FLUSH_UPDATE && COMMIT_ON_CLOSE)",
  "SIMILARITY",
  "(!MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB && !MAX_TOKEN_LENGTH && USE_COMPOUND_FILE && !COMMIT_ON_CLOSE)"
]