[{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"isLockedByCurrentThread()Z","decisionIndex":2,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","!MV_STORE","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.result","className":"LocalResultImpl","methodSignature":"next()Z","decisionIndex":2,"info":["(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStoreTool","methodSignature":"compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V","decisionIndex":4,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"getColumnIndex(Lorg/h2/table/Column;)I","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"getSortIndex()Lorg/h2/index/Index;","decisionIndex":5,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":3,"info":["((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE) || !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Optimizer","methodSignature":"optimize(Z)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType","methodSignature":"getTypeId(Ljava/lang/Object;)I","decisionIndex":3,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore","className":"MVStoreTool","methodSignature":"compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V","decisionIndex":5,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"getValueLen(Lorg/h2/value/Value;Z)I","decisionIndex":18,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"readStoreHeader()V","decisionIndex":22,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseUpdateSetClause(Lorg/h2/command/dml/Update;Lorg/h2/table/TableFilter;IZ)V","decisionIndex":10,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"rollback()V","decisionIndex":4,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Delete","methodSignature":"update()I","decisionIndex":3,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"endTransaction(Lorg/h2/mvstore/tx/Transaction;Z)V","decisionIndex":5,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","!MV_STORE","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command","className":"Command","methodSignature":"start()V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.value","className":"ValueString","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !AUTOCOMMIT && ANALYZE_SAMPLE && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && AUTOCOMMIT && ANALYZE_SAMPLE && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"prepareOrder(Ljava/util/ArrayList;I)Lorg/h2/result/SortOrder;","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"getSortIndex()Lorg/h2/index/Index;","decisionIndex":6,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Optimizer","methodSignature":"optimize(Z)V","decisionIndex":2,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType","methodSignature":"getTypeId(Ljava/lang/Object;)I","decisionIndex":2,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.command","className":"Prepared","methodSignature":"needRecompile()Z","decisionIndex":3,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"asString()Ljava/lang/String;","decisionIndex":5,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseUpdateSetClause(Lorg/h2/command/dml/Update;Lorg/h2/table/TableFilter;IZ)V","decisionIndex":11,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"accountForRemovedPage(IZJJ)Z","decisionIndex":11,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":3,"info":["(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":4,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"compact(I)V","decisionIndex":13,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"RowImpl","methodSignature":"getMemory()I","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"readPage(Lorg/h2/mvstore/MVMap;J)Lorg/h2/mvstore/Page;","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"freeFileSpace(JI)V","decisionIndex":1,"info":["((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE) || !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":5,"info":["(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStoreTool","methodSignature":"compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V","decisionIndex":2,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"balance(Lorg/h2/pagestore/db/TreeNode;Z)V","decisionIndex":12,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Optimizer","methodSignature":"testPlan([Lorg/h2/table/TableFilter;)Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"parseMap(Ljava/lang/String;)Ljava/util/HashMap;","decisionIndex":1,"info":["((MV_STORE && ((!AUTOCOMMIT && !DEFRAG_ALWAYS && ANALYZE_SAMPLE && COMPRESS) || (ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS))) || (!MV_STORE && !DEFRAG_ALWAYS))","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((MV_STORE && ((!AUTOCOMMIT && DEFRAG_ALWAYS && ANALYZE_SAMPLE && COMPRESS) || (ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS))) || (!MV_STORE && DEFRAG_ALWAYS))"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"lock(Lorg/h2/engine/Session;ZZ)Z","decisionIndex":3,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":5,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataNode","methodSignature":"setRowCountStored(I)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"analyzeIfRequired(Lorg/h2/engine/Session;)V","decisionIndex":3,"info":["(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","ANALYZE_AUTO","((FILE_LOCK && ((MV_STORE && !ANALYZE_AUTO) || (ANALYZE_SAMPLE && !ANALYZE_AUTO && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && !ANALYZE_AUTO) || (!MV_STORE && ((ANALYZE_SAMPLE && !ANALYZE_AUTO) || (!ANALYZE_AUTO && !PAGE_SIZE && !ANALYZE_SAMPLE))))))","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"registerTransaction(IILjava/lang/String;JIILorg/h2/mvstore/tx/TransactionStore$RollbackListener;)Lorg/h2/mvstore/tx/Transaction;","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":6,"info":["(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"prepareOrder(Ljava/util/ArrayList;I)Lorg/h2/result/SortOrder;","decisionIndex":3,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"addRowTry(Lorg/h2/result/SearchRow;)I","decisionIndex":2,"info":["(PAGE_SIZE && !AUTOCOMMIT && ANALYZE_SAMPLE && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && AUTOCOMMIT && ANALYZE_SAMPLE && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && AUTOCOMMIT && ANALYZE_SAMPLE && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !AUTOCOMMIT && ANALYZE_SAMPLE && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":69,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((ANALYZE_SAMPLE && !AUTOCOMMIT && FILE_LOCK) || (!FILE_LOCK && ((ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"initialize(Ljava/lang/String;)V","decisionIndex":10,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"getSortIndex()Lorg/h2/index/Index;","decisionIndex":12,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":4,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE) || (ANALYZE_SAMPLE && ((!AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && !AUTOCOMMIT)) && !MV_STORE))","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"readStoreHeader()V","decisionIndex":23,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.command.dml","className":"Update","methodSignature":"update()I","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseUpdateSetClause(Lorg/h2/command/dml/Update;Lorg/h2/table/TableFilter;IZ)V","decisionIndex":9,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"balance(Lorg/h2/pagestore/db/TreeNode;Z)V","decisionIndex":6,"info":["(!ANALYZE_SAMPLE && !PAGE_SIZE && !MV_STORE && FILE_LOCK)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE)","((FILE_LOCK && (MV_STORE || ANALYZE_SAMPLE)) || (!FILE_LOCK && (ANALYZE_SAMPLE || !PAGE_SIZE || MV_STORE)))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"writeStringWithoutLength(Ljava/lang/String;I)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"hasChanges(J)Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && !MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && !MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && !MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && !MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && !MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && !MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"closeStore(ZJ)V","decisionIndex":8,"info":["!DEFRAG_ALWAYS","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore.cache","className":"CacheLongKeyLIRS","methodSignature":"<init>(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Config;)V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataNode","methodSignature":"writeData()V","decisionIndex":3,"info":["(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","((MV_STORE && !DEFRAG_ALWAYS && FILE_LOCK) || (!FILE_LOCK && ((MV_STORE && !DEFRAG_ALWAYS) || (!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !AUTOCOMMIT))))","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE) || (ANALYZE_SAMPLE && ((!AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && !AUTOCOMMIT)) && !MV_STORE))","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"writeStringWithoutLength(Ljava/lang/String;I)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"<init>(Lorg/h2/mvstore/RootReference;JI)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":8,"info":["((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE) || !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"<init>(Lorg/h2/engine/ConnectionInfo;Ljava/lang/String;)V","decisionIndex":3,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"sysIdentifier(Ljava/lang/String;)Ljava/lang/String;","decisionIndex":3,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE) || (ANALYZE_SAMPLE && ((!AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && !AUTOCOMMIT)) && !MV_STORE))","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.expression.aggregate","className":"AggregateDataDefault","methodSignature":"getValue(Lorg/h2/engine/Database;I)Lorg/h2/value/Value;","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.cache","className":"CacheLongKeyLIRS$Segment","methodSignature":"access(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;)V","decisionIndex":3,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!AUTOCOMMIT && DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!AUTOCOMMIT && !DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"lock(Lorg/h2/engine/Session;ZZ)Z","decisionIndex":2,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.util","className":"IntIntHashMap","methodSignature":"get(I)I","decisionIndex":3,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((!AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && !AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.expression","className":"Parameter","methodSignature":"isEverything(Lorg/h2/expression/ExpressionVisitor;)Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Delete","methodSignature":"update()I","decisionIndex":2,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":9,"info":["((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE) || !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"prepare()V","decisionIndex":10,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"registerTransaction(IILjava/lang/String;JIILorg/h2/mvstore/tx/TransactionStore$RollbackListener;)Lorg/h2/mvstore/tx/Transaction;","decisionIndex":4,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeHead()V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"SortOrder","methodSignature":"addExplicitNullPosition(I)I","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"tryLock(I)Lorg/h2/mvstore/RootReference;","decisionIndex":3,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","!MV_STORE","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVStoreTool","methodSignature":"compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V","decisionIndex":7,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":92,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseUpdateSetClause(Lorg/h2/command/dml/Update;Lorg/h2/table/TableFilter;IZ)V","decisionIndex":12,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore","className":"PageFreeList","methodSignature":"allocate(I)V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"updateMeta(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V","decisionIndex":5,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.expression.condition","className":"ConditionAndOr","methodSignature":"optimizeConstant(Lorg/h2/engine/Session;Lorg/h2/expression/Expression;ILorg/h2/expression/Expression;Lorg/h2/expression/Expression;)Lorg/h2/expression/Expression;","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"markStatementEnd()V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","((ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE) || (!MV_STORE && AUTOCOMMIT))","((ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE) || (!MV_STORE && !AUTOCOMMIT))","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"syncLastModificationIdWithDatabase()V","decisionIndex":2,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"isOpen()Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":10,"info":["((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE) || !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"fireRow()Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":25,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeLeaf","methodSignature":"writeHead()V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":2,"info":["((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE) || !MV_STORE)","(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.expression.aggregate","className":"Aggregate","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":4,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"getSortIndex()Lorg/h2/index/Index;","decisionIndex":8,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))) || (!FILE_LOCK && ((MV_STORE && ((COMPRESS && !AUTOCOMMIT) || (!COMPRESS && (ANALYZE_SAMPLE || !AUTOCOMMIT)))) || (!MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT) || (!PAGE_SIZE && !AUTOCOMMIT && !ANALYZE_SAMPLE))))))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","((ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE && FILE_LOCK) || (!MV_STORE && ((ANALYZE_SAMPLE && AUTOCOMMIT) || (!PAGE_SIZE && AUTOCOMMIT && !ANALYZE_SAMPLE)) && !FILE_LOCK))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore.tx","className":"RollbackDecisionMaker","methodSignature":"decide([Ljava/lang/Object;[Ljava/lang/Object;)Lorg/h2/mvstore/MVMap$Decision;","decisionIndex":2,"info":["(COMPRESS && ANALYZE_SAMPLE && MV_STORE)","!MV_STORE","(!COMPRESS && ANALYZE_SAMPLE && MV_STORE)","(!COMPRESS && !ANALYZE_SAMPLE && MV_STORE)","(COMPRESS && !ANALYZE_SAMPLE && MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataNode","methodSignature":"writeData()V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","((MV_STORE && !DEFRAG_ALWAYS && FILE_LOCK) || (!FILE_LOCK && ((MV_STORE && !DEFRAG_ALWAYS) || (!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !AUTOCOMMIT))))","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":10,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","((MV_STORE && ((ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS) || (!COMPRESS && ANALYZE_SAMPLE))) || (ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"accountForRemovedPage(IZJJ)Z","decisionIndex":9,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"writeValue(Lorg/h2/value/Value;)V","decisionIndex":36,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"IntIntHashMap","methodSignature":"get(I)I","decisionIndex":5,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((!AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && !AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && ((AUTOCOMMIT && !DEFRAG_ALWAYS && PAGE_SIZE) || (!PAGE_SIZE && AUTOCOMMIT)) && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"doLock2(Lorg/h2/engine/Session;IZ)Z","decisionIndex":2,"info":["(MV_STORE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && AUTOCOMMIT && !MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !AUTOCOMMIT && !MV_STORE)","(MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"addTableToDependencies(Lorg/h2/mvstore/db/MVTable;Ljava/util/HashSet;)V","decisionIndex":2,"info":["(ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && COMPRESS && MV_STORE)","((ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE) || (!MV_STORE && AUTOCOMMIT))","((ANALYZE_SAMPLE && !AUTOCOMMIT && MV_STORE) || (!MV_STORE && !AUTOCOMMIT))","(!ANALYZE_SAMPLE && AUTOCOMMIT && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && AUTOCOMMIT && COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !AUTOCOMMIT && !COMPRESS && MV_STORE)"]}]