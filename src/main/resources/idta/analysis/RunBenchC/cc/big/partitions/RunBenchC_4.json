[{"packageName":"org.h2.value","className":"ValueInt","methodSignature":"checkRange(J)Lorg/h2/value/ValueInt;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"appendMap(Ljava/lang/StringBuilder;Ljava/lang/String;Ljava/lang/String;)V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"checkOpen(I)V","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataIndex","methodSignature":"add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"prepare()V","decisionIndex":10,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"getHigherOrder(II)I","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"IntIntHashMap","methodSignature":"remove(I)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"beforeWrite(Lorg/h2/mvstore/MVMap;)V","decisionIndex":6,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readCondition()Lorg/h2/expression/Expression;","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"getLobStorage()Lorg/h2/store/LobStorageInterface;","decisionIndex":1,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.table","className":"TableBase","methodSignature":"getCreateSQL()Ljava/lang/String;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"getSearchRow(Lorg/h2/result/Row;)Lorg/h2/result/SearchRow;","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"get(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Ljava/lang/Object;","decisionIndex":3,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":12,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.cache","className":"CacheLongKeyLIRS$Segment","methodSignature":"access(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"getChunk(J)Lorg/h2/mvstore/Chunk;","decisionIndex":1,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"balance(Lorg/h2/pagestore/db/TreeNode;Z)V","decisionIndex":12,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":11,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeCursor","methodSignature":"next(Lorg/h2/pagestore/db/TreeNode;)Lorg/h2/pagestore/db/TreeNode;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"prepare()V","decisionIndex":11,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"deregisterVersionUsage(Lorg/h2/mvstore/MVStore$TxCounter;)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"beforeWrite(Lorg/h2/mvstore/MVMap;)V","decisionIndex":7,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"convertToInt(JLjava/lang/Object;)I","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"beforeWrite(Lorg/h2/mvstore/MVMap;)V","decisionIndex":8,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"read()V","decisionIndex":4,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || (!DEFRAG_ALWAYS && !PAGE_SIZE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"getSearchRow(Lorg/h2/result/Row;)Lorg/h2/result/SearchRow;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataIndex","methodSignature":"add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":3,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TxDecisionMaker","methodSignature":"decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"hasChangesSince(J)Z","decisionIndex":4,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":11,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.expression","className":"Expression","methodSignature":"getUnenclosedSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"addMeta(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"operate(Ljava/lang/Object;Ljava/lang/Object;Lorg/h2/mvstore/MVMap$DecisionMaker;)Ljava/lang/Object;","decisionIndex":21,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"updateRows(Lorg/h2/command/Prepared;Lorg/h2/engine/Session;Lorg/h2/result/RowList;)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"readFully(Ljava/nio/channels/FileChannel;JLjava/nio/ByteBuffer;)V","decisionIndex":2,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"readStoreHeader()V","decisionIndex":33,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.table","className":"TableBase","methodSignature":"getCreateSQL()Ljava/lang/String;","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"read()V","decisionIndex":3,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || (!DEFRAG_ALWAYS && !PAGE_SIZE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"markStatementStart(Ljava/util/HashSet;Ljava/util/HashSet;)V","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"IndexCursor","methodSignature":"prepare(Lorg/h2/engine/Session;Ljava/util/ArrayList;)V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"hasChangesSince(J)Z","decisionIndex":3,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"getAdditionalAnd(Lorg/h2/engine/Session;Lorg/h2/expression/condition/Comparison;)Lorg/h2/expression/Expression;","decisionIndex":8,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"getCostRangeIndex([IJ[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;ZLorg/h2/command/dml/AllColumnsForPlan;)J","decisionIndex":25,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"operate(Ljava/lang/Object;Ljava/lang/Object;Lorg/h2/mvstore/MVMap$DecisionMaker;)Ljava/lang/Object;","decisionIndex":22,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"readStoreHeader()V","decisionIndex":32,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"read()V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || (!DEFRAG_ALWAYS && !PAGE_SIZE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"<init>(Lorg/h2/command/ddl/CreateTableData;)V","decisionIndex":1,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"hasChangesSince(J)Z","decisionIndex":2,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readCondition()Lorg/h2/expression/Expression;","decisionIndex":6,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"operate(Ljava/lang/Object;Ljava/lang/Object;Lorg/h2/mvstore/MVMap$DecisionMaker;)Ljava/lang/Object;","decisionIndex":19,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"CommandContainer","methodSignature":"query(I)Lorg/h2/result/ResultInterface;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"edu.cmu.cs.mvelezce.bench.c","className":"BenchCThread","methodSignature":"processOrderStatus()V","decisionIndex":8,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"check()V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"read()V","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || (!DEFRAG_ALWAYS && !PAGE_SIZE))","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"getRowSize(Lorg/h2/store/Data;Lorg/h2/result/SearchRow;Z)I","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE)","(MV_STORE && PAGE_SIZE)"]},{"packageName":"edu.cmu.cs.mvelezce.bench.c","className":"BenchCThread","methodSignature":"processOrderStatus()V","decisionIndex":7,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"hasChangesSince(J)Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"getAdditionalAnd(Lorg/h2/engine/Session;Lorg/h2/expression/condition/Comparison;)Lorg/h2/expression/Expression;","decisionIndex":6,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":8,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"markTransactionEnd()V","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.store.fs","className":"FilePathDisk","methodSignature":"delete()V","decisionIndex":2,"info":["(FILE_LOCK && !DEFRAG_ALWAYS)","(FILE_LOCK && DEFRAG_ALWAYS)","(!FILE_LOCK && !DEFRAG_ALWAYS)","(!FILE_LOCK && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.util","className":"CacheLRU","methodSignature":"setMaxMemory(I)V","decisionIndex":1,"info":["(!CACHE_SIZE && !MV_STORE)","MV_STORE","(CACHE_SIZE && !MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"FreeSpaceBitSet","methodSignature":"allocate(IIIZ)I","decisionIndex":11,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"preparePlan(Z)D","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"checkPrecision(J)Z","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"getHigherOrder(II)I","decisionIndex":6,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"RowImpl","methodSignature":"getMemory()I","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"findOldChunks(I)Ljava/lang/Iterable;","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"getBestPlanItem(Lorg/h2/engine/Session;[Lorg/h2/table/TableFilter;ILorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;","decisionIndex":5,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"operate(Ljava/lang/Object;Ljava/lang/Object;Lorg/h2/mvstore/MVMap$DecisionMaker;)Ljava/lang/Object;","decisionIndex":23,"info":["!MV_STORE","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"getAdditionalAnd(Lorg/h2/engine/Session;Lorg/h2/expression/condition/Comparison;)Lorg/h2/expression/Expression;","decisionIndex":5,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":15,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"FreeSpaceBitSet","methodSignature":"allocate(IIIZ)I","decisionIndex":12,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Delete","methodSignature":"update()I","decisionIndex":18,"info":["(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Cursor","methodSignature":"traverseDown(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Lorg/h2/mvstore/CursorPos;","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"readStoreHeader()V","decisionIndex":37,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"updateRows(Lorg/h2/command/Prepared;Lorg/h2/engine/Session;Lorg/h2/result/RowList;)V","decisionIndex":5,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readCondition()Lorg/h2/expression/Expression;","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"updateRowCount(I)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":16,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"LocalResultImpl","methodSignature":"createShallowCopy(Lorg/h2/engine/SessionInterface;)Lorg/h2/result/LocalResultImpl;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType$IntegerType","methodSignature":"getMemory(Ljava/lang/Object;)I","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TxDecisionMaker","methodSignature":"isThisTransaction(I)Z","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"writePage(ILorg/h2/store/Data;)V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueString","methodSignature":"get(Ljava/lang/String;Lorg/h2/engine/CastDataProvider;)Lorg/h2/value/Value;","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"appendMap(Ljava/lang/StringBuilder;Ljava/lang/String;Ljava/lang/String;)V","decisionIndex":2,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readSum()Lorg/h2/expression/Expression;","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"setStatus(I)J","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueLong","methodSignature":"get(J)Lorg/h2/value/ValueLong;","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && COMPRESS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && COMPRESS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && COMPRESS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && COMPRESS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)","(PAGE_SIZE && COMPRESS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"getHigherOrder(II)I","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSelectExpressions(Lorg/h2/command/dml/Select;)V","decisionIndex":5,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"scrubMetaMap()V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"getBestPlanItem(Lorg/h2/engine/Session;[Lorg/h2/table/TableFilter;ILorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":7,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":17,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"getAdditionalAnd(Lorg/h2/engine/Session;Lorg/h2/expression/condition/Comparison;)Lorg/h2/expression/Expression;","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"moveChild(II)V","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]}]