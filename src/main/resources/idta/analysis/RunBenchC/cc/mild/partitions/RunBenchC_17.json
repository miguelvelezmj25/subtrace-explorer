[{"packageName":"org.h2.value","className":"ValueDecimal","methodSignature":"<init>(Ljava/math/BigDecimal;)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"increaseFileSize()V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"dropUnusedVersions()V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"getCostRangeIndex([IJ[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;ZLorg/h2/command/dml/AllColumnsForPlan;)J","decisionIndex":4,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"waitIfExclusiveModeEnabled()V","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"initialize(Ljava/lang/String;)V","decisionIndex":31,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"readString(I)Ljava/lang/String;","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"split(I)Lorg/h2/mvstore/Page;","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageOutputStream","methodSignature":"freeReserved()V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"closeStore(ZJ)V","decisionIndex":9,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"getCache()[Ljava/lang/String;","decisionIndex":1,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":7,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"DateTimeUtils","methodSignature":"convertScale(JIJ)J","decisionIndex":3,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"insertRows()I","decisionIndex":4,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"trimSubstring(Ljava/lang/String;II)Ljava/lang/String;","decisionIndex":3,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.compress","className":"CompressLZF","methodSignature":"compress([BI[BI)I","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(DEFRAG_ALWAYS && !COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":7,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageFreeList","methodSignature":"allocate(Ljava/util/BitSet;I)I","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"readString(I)Ljava/lang/String;","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"DateTimeUtils","methodSignature":"dateValueFromAbsoluteDay(J)J","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"getDatabasePath()Ljava/lang/String;","decisionIndex":1,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"split(I)Lorg/h2/mvstore/Page;","decisionIndex":4,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueInt","methodSignature":"checkRange(J)Lorg/h2/value/ValueInt;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"CursorPos","methodSignature":"traverseDown(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Lorg/h2/mvstore/CursorPos;","decisionIndex":2,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$Leaf","methodSignature":"writeUnsavedRecursive(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"isIdle()Z","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"isLockedByCurrentThread()Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"insertRows()I","decisionIndex":5,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"FileStore","methodSignature":"open(Ljava/lang/String;Z[C)V","decisionIndex":5,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.pagestore.db","className":"PageDataIndex","methodSignature":"close(Lorg/h2/engine/Session;)V","decisionIndex":1,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"analyzeIfRequired(Lorg/h2/engine/Session;)V","decisionIndex":2,"info":["ANALYZE_AUTO","(AUTOCOMMIT && !ANALYZE_AUTO)","(!AUTOCOMMIT && !ANALYZE_AUTO)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"trimSubstring(Ljava/lang/String;II)Ljava/lang/String;","decisionIndex":2,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.store.fs","className":"FilePath","methodSignature":"get(Ljava/lang/String;)Lorg/h2/store/fs/FilePath;","decisionIndex":1,"info":["(!MV_STORE && DEFRAG_ALWAYS && FILE_LOCK)","(!MV_STORE && !DEFRAG_ALWAYS && !FILE_LOCK)","(MV_STORE && !DEFRAG_ALWAYS && !FILE_LOCK)","(MV_STORE && DEFRAG_ALWAYS && !FILE_LOCK)","(!MV_STORE && DEFRAG_ALWAYS && !FILE_LOCK)","(MV_STORE && !DEFRAG_ALWAYS && FILE_LOCK)","(!MV_STORE && !DEFRAG_ALWAYS && FILE_LOCK)","(MV_STORE && DEFRAG_ALWAYS && FILE_LOCK)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"sameResultAsLast(Lorg/h2/engine/Session;[Lorg/h2/value/Value;[Lorg/h2/value/Value;J)Z","decisionIndex":3,"info":["AUTOCOMMIT","!AUTOCOMMIT"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":9,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"writeBack(Lorg/h2/util/CacheObject;)V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"gatherGroup(II)V","decisionIndex":6,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"cache(Lorg/h2/value/Value;)Lorg/h2/value/Value;","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"IntArray","methodSignature":"removeRange(II)V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType$LongType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V","decisionIndex":6,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":22,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":10,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"trimSubstring(Ljava/lang/String;II)Ljava/lang/String;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStreamTrunk","methodSignature":"write()V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && PAGE_SIZE)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"IntIntHashMap","methodSignature":"remove(I)V","decisionIndex":2,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.store.fs","className":"FilePathDisk","methodSignature":"translateFileName(Ljava/lang/String;)Ljava/lang/String;","decisionIndex":1,"info":["(!MV_STORE && DEFRAG_ALWAYS && FILE_LOCK)","(!MV_STORE && !DEFRAG_ALWAYS && !FILE_LOCK)","(MV_STORE && !DEFRAG_ALWAYS && !FILE_LOCK)","(MV_STORE && DEFRAG_ALWAYS && !FILE_LOCK)","(!MV_STORE && DEFRAG_ALWAYS && !FILE_LOCK)","(MV_STORE && !DEFRAG_ALWAYS && FILE_LOCK)","(!MV_STORE && !DEFRAG_ALWAYS && FILE_LOCK)","(MV_STORE && DEFRAG_ALWAYS && FILE_LOCK)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"closeStore(ZJ)V","decisionIndex":7,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"initialize(Ljava/lang/String;)V","decisionIndex":29,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"cache(Lorg/h2/value/Value;)Lorg/h2/value/Value;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":9,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"rebuildIndex(Lorg/h2/engine/Session;Lorg/h2/mvstore/db/MVIndex;Ljava/lang/String;)V","decisionIndex":1,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":23,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":3,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"read()V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression","className":"ExpressionColumn","methodSignature":"isEverything(Lorg/h2/expression/ExpressionVisitor;)Z","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"insertRows()I","decisionIndex":7,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"setPageSize(I)V","decisionIndex":5,"info":["(!MV_STORE && PAGE_SIZE)","MV_STORE","(!MV_STORE && !PAGE_SIZE)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readIdentifierWithSchema(Ljava/lang/String;)Ljava/lang/String;","decisionIndex":1,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"rollback()V","decisionIndex":1,"info":["(MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"VersionedValueCommitted","methodSignature":"getInstance(Ljava/lang/Object;)Lorg/h2/value/VersionedValue;","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":9,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readAnd(Lorg/h2/expression/Expression;)Lorg/h2/expression/Expression;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":24,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"<init>(JJ)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !PAGE_SIZE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && !PAGE_SIZE && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.compress","className":"CompressLZF","methodSignature":"compress([BI[BI)I","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(DEFRAG_ALWAYS && !COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"addRow(Lorg/h2/result/SearchRow;)V","decisionIndex":2,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"setPageSize(I)V","decisionIndex":4,"info":["(!MV_STORE && PAGE_SIZE)","MV_STORE","(!MV_STORE && !PAGE_SIZE)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":3,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compact(II)Z","decisionIndex":4,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":11,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"CommitDecisionMaker","methodSignature":"selectValue(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/value/VersionedValue;","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":25,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"<init>(JJ)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !PAGE_SIZE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && !PAGE_SIZE && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"writeVarInt(I)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"isLive()Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I","decisionIndex":8,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"RowImpl","methodSignature":"getByteCount(Lorg/h2/store/Data;)I","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"closeStore(ZJ)V","decisionIndex":4,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"shrinkFileIfPossible(I)V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.util","className":"DateTimeUtils","methodSignature":"dateValueFromAbsoluteDay(J)J","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"setPageSize(I)V","decisionIndex":3,"info":["(!MV_STORE && PAGE_SIZE)","MV_STORE","(!MV_STORE && !PAGE_SIZE)"]},{"packageName":"org.h2.engine","className":"User","methodSignature":"checkRight(Lorg/h2/table/Table;I)V","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"getBestPlanItem(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;","decisionIndex":2,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"getPage(I)Lorg/h2/pagestore/Page;","decisionIndex":6,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"doMaintenance(I)V","decisionIndex":4,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"parseMap(Ljava/lang/String;)Ljava/util/HashMap;","decisionIndex":2,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"updateRowCount(I)V","decisionIndex":1,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compact(II)Z","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"DateTimeUtils","methodSignature":"dateValueFromAbsoluteDay(J)J","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"split(I)Lorg/h2/mvstore/Page;","decisionIndex":5,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageLog","methodSignature":"removeUntil(I)V","decisionIndex":2,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":4,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"closeStore(ZJ)V","decisionIndex":11,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"read()V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"SimpleRow","methodSignature":"getMemory()I","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"isActive(I)Z","decisionIndex":3,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","!MV_STORE","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"FileStore","methodSignature":"open(Ljava/lang/String;Z[C)V","decisionIndex":2,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.value","className":"ValueDecimal","methodSignature":"checkPrecision(J)Z","decisionIndex":1,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.compress","className":"CompressLZF","methodSignature":"compress([BI[BI)I","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(DEFRAG_ALWAYS && !COMPRESS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !COMPRESS && MV_STORE)"]}]