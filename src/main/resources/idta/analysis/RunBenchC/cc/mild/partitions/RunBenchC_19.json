[{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"append(Ljava/lang/Object;Ljava/lang/Object;)V","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"Column","methodSignature":"getCreateSQL(Z)Ljava/lang/String;","decisionIndex":7,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"recalculateMemory()V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"getValueLen(Lorg/h2/value/Value;Z)I","decisionIndex":11,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":17,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"analyzeIfRequired(Lorg/h2/engine/Session;)V","decisionIndex":3,"info":["(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE || ANALYZE_AUTO)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"get(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Ljava/lang/Object;","decisionIndex":3,"info":["(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && COMPRESS && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && COMPRESS && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && COMPRESS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"IntArray","methodSignature":"add(I)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionMap","methodSignature":"set(Ljava/lang/Object;Lorg/h2/mvstore/tx/TxDecisionMaker;)Ljava/lang/Object;","decisionIndex":1,"info":["(!AUTOCOMMIT && MV_STORE)","(!AUTOCOMMIT && !MV_STORE)","(AUTOCOMMIT && MV_STORE)","(AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"asString(Ljava/lang/String;)Ljava/lang/String;","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"query(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":8,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && AUTOCOMMIT && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && !AUTOCOMMIT && FILE_LOCK)","(MV_STORE && PAGE_SIZE && AUTOCOMMIT && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && AUTOCOMMIT && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && !AUTOCOMMIT && FILE_LOCK)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && !AUTOCOMMIT && !FILE_LOCK)","(MV_STORE && PAGE_SIZE && AUTOCOMMIT && FILE_LOCK)","(MV_STORE && PAGE_SIZE && !AUTOCOMMIT && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":12,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.cache","className":"CacheLongKeyLIRS$Segment","methodSignature":"access(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"VersionedValueType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"getPage(I)Lorg/h2/pagestore/db/PageBtree;","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":17,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"addRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":1,"info":["AUTOCOMMIT","!AUTOCOMMIT"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"updateRows(Lorg/h2/command/Prepared;Lorg/h2/engine/Session;Lorg/h2/result/RowList;)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"setRowCountStored(I)V","decisionIndex":4,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"isRewritable()Z","decisionIndex":4,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"log(ILjava/lang/Object;Lorg/h2/value/VersionedValue;)J","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"read(Ljava/lang/String;)V","decisionIndex":2,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"VersionedValueType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":2,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore","className":"MVStoreTool","methodSignature":"compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V","decisionIndex":2,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":19,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.message","className":"Trace","methodSignature":"isEnabled(I)Z","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"Column","methodSignature":"validateConvertUpdateSequence(Lorg/h2/engine/Session;Lorg/h2/value/Value;)Lorg/h2/value/Value;","decisionIndex":8,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"addRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":77,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Prepared","methodSignature":"needRecompile()Z","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$Leaf","methodSignature":"insertLeaf(ILjava/lang/Object;Ljava/lang/Object;)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":14,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStoreTool","methodSignature":"compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V","decisionIndex":1,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.command.dml","className":"AllColumnsForPlan","methodSignature":"add(Lorg/h2/table/Column;)V","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"VersionedValueType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"<init>(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/type/DataType;Lorg/h2/mvstore/type/DataType;IJLjava/util/concurrent/atomic/AtomicReference;IZ)V","decisionIndex":2,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.expression.condition","className":"ConditionAndOr","methodSignature":"createIndexConditions(Lorg/h2/engine/Session;Lorg/h2/table/TableFilter;)V","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && PAGE_SIZE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !PAGE_SIZE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TxDecisionMaker","methodSignature":"decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":76,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"rowForResult([Lorg/h2/value/Value;I)[Lorg/h2/value/Value;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"VersionedValueType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":4,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":5,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"FileLock","methodSignature":"run()V","decisionIndex":1,"info":["!FILE_LOCK","FILE_LOCK"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"isRewritable()Z","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":6,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.store.fs","className":"FileUtils","methodSignature":"readFully(Ljava/nio/channels/FileChannel;Ljava/nio/ByteBuffer;)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"edu.cmu.cs.mvelezce.bench.c","className":"BenchCThread","methodSignature":"processNewOrder()V","decisionIndex":13,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionMap","methodSignature":"sizeAsLong()J","decisionIndex":3,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"compareWithNull(Lorg/h2/value/Value;ZLorg/h2/engine/CastDataProvider;Lorg/h2/value/CompareMode;)I","decisionIndex":5,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVPrimaryIndex","methodSignature":"update(Lorg/h2/engine/Session;Lorg/h2/result/Row;Lorg/h2/result/Row;)V","decisionIndex":13,"info":["(!AUTOCOMMIT && MV_STORE)","!MV_STORE","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"registerTransaction(IILjava/lang/String;JIILorg/h2/mvstore/tx/TransactionStore$RollbackListener;)Lorg/h2/mvstore/tx/Transaction;","decisionIndex":4,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"VersionedValueType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":5,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":2,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType$LongType","methodSignature":"getMemory(Ljava/lang/Object;)I","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"composeState(IJZ)J","decisionIndex":3,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","!MV_STORE","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"removeMetaIndex(Lorg/h2/index/Index;Lorg/h2/engine/Session;)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"checkOpen()V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeLeaf","methodSignature":"writeData()V","decisionIndex":5,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"finishResult(Lorg/h2/result/LocalResult;JIZLorg/h2/result/ResultTarget;)Lorg/h2/result/LocalResult;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"addMeta(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"edu.cmu.cs.mvelezce.bench.c","className":"BenchCThread","methodSignature":"processNewOrder()V","decisionIndex":14,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.aggregate","className":"AggregateDataSelectivity","methodSignature":"getValue(Lorg/h2/engine/Database;I)Lorg/h2/value/Value;","decisionIndex":2,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"findSchema(Ljava/lang/String;)Lorg/h2/schema/Schema;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"finishResult(Lorg/h2/result/LocalResult;JIZLorg/h2/result/ResultTarget;)Lorg/h2/result/LocalResult;","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"gatherSnapshots(Ljava/util/HashSet;ZLjava/util/Map;)V","decisionIndex":2,"info":["(AUTOCOMMIT && ANALYZE_SAMPLE)","(!AUTOCOMMIT && !ANALYZE_SAMPLE)","(AUTOCOMMIT && !ANALYZE_SAMPLE)","(!AUTOCOMMIT && ANALYZE_SAMPLE)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"calculateTotalCount()J","decisionIndex":1,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCommit()Lorg/h2/command/dml/TransactionCommand;","decisionIndex":1,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeLeaf","methodSignature":"writeData()V","decisionIndex":4,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"FileStore","methodSignature":"setLength(J)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"isRewritable()Z","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":5,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore$RemovedPageInfo","methodSignature":"isPinned()Z","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readExpressionWithGlobalConditions()Lorg/h2/expression/Expression;","decisionIndex":2,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionMap","methodSignature":"set(Ljava/lang/Object;Lorg/h2/mvstore/tx/TxDecisionMaker;)Ljava/lang/Object;","decisionIndex":3,"info":["(!AUTOCOMMIT && MV_STORE)","(!AUTOCOMMIT && !MV_STORE)","(AUTOCOMMIT && MV_STORE)","(AUTOCOMMIT && !MV_STORE)"]},{"packageName":"org.h2.pagestore","className":"PageOutputStream","methodSignature":"initNextData()V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE || !PAGE_SIZE)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"read(Ljava/lang/String;)V","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"commit(Z)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":16,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"Page","methodSignature":"insert([Ljava/lang/Object;IILjava/lang/Object;)[Ljava/lang/Object;","decisionIndex":2,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":19,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"writeRow(Lorg/h2/store/Data;ILorg/h2/result/SearchRow;Z)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"cache(Ljava/lang/String;)Ljava/lang/String;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.jdbc","className":"JdbcPreparedStatement","methodSignature":"<init>(Lorg/h2/jdbc/JdbcConnection;Ljava/lang/String;IIIZLjava/lang/Object;)V","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"gatherSnapshots(Ljava/util/HashSet;ZLjava/util/Map;)V","decisionIndex":3,"info":["(AUTOCOMMIT && ANALYZE_SAMPLE)","(!AUTOCOMMIT && !ANALYZE_SAMPLE)","(AUTOCOMMIT && !ANALYZE_SAMPLE)","(!AUTOCOMMIT && ANALYZE_SAMPLE)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"updateMeta(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V","decisionIndex":3,"info":["(!AUTOCOMMIT && MV_STORE)","!MV_STORE","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.expression.aggregate","className":"AggregateDataCollecting","methodSignature":"add(Lorg/h2/engine/Database;Lorg/h2/value/Value;)V","decisionIndex":2,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"checkPrecision(J)Z","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"isRewritable()Z","decisionIndex":2,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readExpressionWithGlobalConditions()Lorg/h2/expression/Expression;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":4,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"readPage(ILorg/h2/store/Data;)V","decisionIndex":5,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(MV_STORE && !DEFRAG_ALWAYS)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"createIndexConditions(Lorg/h2/engine/Session;Lorg/h2/table/TableFilter;)V","decisionIndex":4,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"getFletcher32([BII)I","decisionIndex":3,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setWriteVersion(J)V","decisionIndex":1,"info":["!DEFRAG_ALWAYS","DEFRAG_ALWAYS"]},{"packageName":"org.h2.value","className":"ValueTimestamp","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !PAGE_SIZE && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && !PAGE_SIZE && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]}]