[{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"sameResultAsLast(Lorg/h2/engine/Session;[Lorg/h2/value/Value;[Lorg/h2/value/Value;J)Z","decisionIndex":4,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"readVarLong()J","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactRewrite(Ljava/util/Set;)I","decisionIndex":7,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"appendMap(Ljava/lang/StringBuilder;Ljava/lang/String;Ljava/lang/String;)V","decisionIndex":1,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"doLock2(Lorg/h2/engine/Session;IZ)Z","decisionIndex":1,"info":["(!AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"open(IILorg/h2/engine/ConnectionInfo;)V","decisionIndex":31,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"compare(Ljava/lang/Object;Ljava/lang/Object;)I","decisionIndex":1,"info":["(ANALYZE_SAMPLE && !COMPRESS && !MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && !MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && !MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && !MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && !MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"<init>(Ljava/util/Map;)V","decisionIndex":3,"info":["(MV_STORE && !DEFRAG_ALWAYS)","(MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"get(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Ljava/lang/Object;","decisionIndex":1,"info":["(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"removeAllRecursive(J)I","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"swap(III)V","decisionIndex":3,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":13,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"binarySearch(Ljava/lang/Object;)I","decisionIndex":5,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":8,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"tryCommit()J","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"open(IILorg/h2/engine/ConnectionInfo;)V","decisionIndex":32,"info":["!MV_STORE","MV_STORE"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":29,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;","decisionIndex":3,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":16,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"CacheLRU","methodSignature":"<init>(Lorg/h2/util/CacheWriter;IZ)V","decisionIndex":1,"info":["(!CACHE_SIZE && !MV_STORE)","MV_STORE","(CACHE_SIZE && !MV_STORE)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readColumnIdentifier()Ljava/lang/String;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"ConditionAndOr","methodSignature":"getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;","decisionIndex":6,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"split(I)Lorg/h2/mvstore/Page;","decisionIndex":4,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readTableFilter()Lorg/h2/table/TableFilter;","decisionIndex":6,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.store.fs","className":"FileDisk","methodSignature":"read(Ljava/nio/ByteBuffer;)I","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"compact(I)V","decisionIndex":19,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"isIdle()Z","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"UndoLogRecord","methodSignature":"undo(Lorg/h2/engine/Session;)V","decisionIndex":1,"info":["(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE)","MV_STORE"]},{"packageName":"org.h2.value","className":"ValueTimestampTimeZone","methodSignature":"<init>(JJI)V","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"isOk(Lorg/h2/expression/Expression;)Z","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"analyzeIfRequired(Lorg/h2/engine/Session;)V","decisionIndex":2,"info":["(PAGE_SIZE && !ANALYZE_AUTO && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE)","(!PAGE_SIZE && !ANALYZE_AUTO && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE)","(!PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !ANALYZE_AUTO && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !AUTOCOMMIT)","ANALYZE_AUTO","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !ANALYZE_AUTO && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !ANALYZE_AUTO && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"TreeIndex","methodSignature":"child(Lorg/h2/pagestore/db/TreeNode;Z)Lorg/h2/pagestore/db/TreeNode;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","MV_STORE"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"tryLock(I)Lorg/h2/mvstore/RootReference;","decisionIndex":3,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"clearIt()Lorg/h2/mvstore/RootReference;","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"TableBase","methodSignature":"getCreateSQL()Ljava/lang/String;","decisionIndex":9,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactRewrite(Ljava/util/Set;)I","decisionIndex":8,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"compact(I)V","decisionIndex":20,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK)"]},{"packageName":"org.h2.value","className":"ValueTimestampTimeZone","methodSignature":"<init>(JJI)V","decisionIndex":4,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueInt","methodSignature":"get(I)Lorg/h2/value/ValueInt;","decisionIndex":1,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"acceptChunkOccupancyChanges(JJ)V","decisionIndex":6,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"DataType","methodSignature":"isDateTimeType(I)Z","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"readValue(Ljava/nio/ByteBuffer;)Ljava/lang/Object;","decisionIndex":10,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","!MV_STORE","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":2,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"removeAllRecursive(J)I","decisionIndex":2,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"removePage(J)I","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore$ArrayType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"edu.cmu.cs.mvelezce.bench.c","className":"BenchCThread","methodSignature":"processOrderStatus()V","decisionIndex":9,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"RollbackDecisionMaker","methodSignature":"decide([Ljava/lang/Object;[Ljava/lang/Object;)Lorg/h2/mvstore/MVMap$Decision;","decisionIndex":3,"info":["!MV_STORE","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V","decisionIndex":2,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepare(Ljava/lang/String;)Lorg/h2/command/Prepared;","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select$LazyResultQueryFlat","methodSignature":"fetchNextRow()[Lorg/h2/value/Value;","decisionIndex":7,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"CommitDecisionMaker","methodSignature":"decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"commit()V","decisionIndex":1,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"queryWithoutCache(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":36,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":1,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(AUTOCOMMIT && !MV_STORE)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"log(Lorg/h2/table/Table;SLorg/h2/result/Row;)V","decisionIndex":2,"info":["(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && FILE_LOCK)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !FILE_LOCK)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","MV_STORE","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"isQuery()Z","decisionIndex":2,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"nextPage(Lorg/h2/pagestore/db/PageBtreeCursor;I)V","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":18,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.jdbc","className":"JdbcResultSet","methodSignature":"get(I)Lorg/h2/value/Value;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"init()V","decisionIndex":18,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Update","methodSignature":"update()I","decisionIndex":7,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueTimestampTimeZone","methodSignature":"<init>(JJI)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"store(JJ)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.cache","className":"CacheLongKeyLIRS$Segment","methodSignature":"access(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;)V","decisionIndex":5,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"getPage(I)Lorg/h2/pagestore/Page;","decisionIndex":7,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":10,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"swap(III)V","decisionIndex":6,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"removePage(J)I","decisionIndex":3,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"isQuery()Z","decisionIndex":1,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"isLive()Z","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"getFreeList(I)Lorg/h2/pagestore/PageFreeList;","decisionIndex":3,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":26,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"read(Ljava/nio/ByteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":1,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","!MV_STORE","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"rollback()V","decisionIndex":2,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageOutputStream","methodSignature":"write([BII)V","decisionIndex":2,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"queryWithoutCache(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":38,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"isActive(I)Z","decisionIndex":2,"info":["(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","!MV_STORE","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeLeaf","methodSignature":"writeData()V","decisionIndex":1,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"getPage(I)Lorg/h2/pagestore/Page;","decisionIndex":6,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;","decisionIndex":4,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":11,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"swap(III)V","decisionIndex":5,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"split(I)Lorg/h2/mvstore/Page;","decisionIndex":5,"info":["(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","!MV_STORE","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && AUTOCOMMIT)","(ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(!ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)","(!ANALYZE_SAMPLE && !COMPRESS && MV_STORE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":3,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"ConditionAndOr","methodSignature":"getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;","decisionIndex":5,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"getFreeList(I)Lorg/h2/pagestore/PageFreeList;","decisionIndex":4,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!DEFRAG_ALWAYS && PAGE_SIZE && !MV_STORE)","MV_STORE","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"markAsRemoved()Z","decisionIndex":3,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageOutputStream","methodSignature":"write([BII)V","decisionIndex":1,"info":["(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && MV_STORE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"next()Z","decisionIndex":15,"info":["(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && FILE_LOCK && AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":5,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && MV_STORE)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && MV_STORE)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"writeVarInt(Ljava/nio/ByteBuffer;I)V","decisionIndex":1,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"readHexLong(Ljava/util/Map;Ljava/lang/String;J)J","decisionIndex":1,"info":["(!DEFRAG_ALWAYS && !ANALYZE_SAMPLE && MV_STORE)","(DEFRAG_ALWAYS && !ANALYZE_SAMPLE && MV_STORE)","(DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE)","(!MV_STORE && DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && ANALYZE_SAMPLE && MV_STORE)","(!MV_STORE && !DEFRAG_ALWAYS)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":7,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","MV_STORE","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && !DEFRAG_ALWAYS && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !DEFRAG_ALWAYS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !DEFRAG_ALWAYS && !FILE_LOCK && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":4,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(MV_STORE && ANALYZE_SAMPLE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"swap(III)V","decisionIndex":4,"info":["(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE || !DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && CACHE_SIZE && !FILE_LOCK && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && ANALYZE_SAMPLE && !CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && ANALYZE_SAMPLE && !CACHE_SIZE && PAGE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.ddl","className":"Analyze","methodSignature":"analyzeTable(Lorg/h2/engine/Session;Lorg/h2/table/Table;IZ)V","decisionIndex":12,"info":["(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(!AUTOCOMMIT && MV_STORE)","(!PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && !ANALYZE_SAMPLE && !MV_STORE && !AUTOCOMMIT)","(PAGE_SIZE && ANALYZE_SAMPLE && !MV_STORE && AUTOCOMMIT)","(AUTOCOMMIT && MV_STORE)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"rollbackTo(Lorg/h2/mvstore/tx/Transaction;JJ)V","decisionIndex":1,"info":["(MV_STORE && ANALYZE_SAMPLE)","!MV_STORE","(MV_STORE && !ANALYZE_SAMPLE)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":27,"info":["(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && !DEFRAG_ALWAYS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && AUTOCOMMIT)","(COMPRESS && MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !DEFRAG_ALWAYS)","(!DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !COMPRESS && !AUTOCOMMIT)","(MV_STORE && !ANALYZE_SAMPLE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && DEFRAG_ALWAYS)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && AUTOCOMMIT)","(COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !ANALYZE_SAMPLE && !AUTOCOMMIT)","(ANALYZE_SAMPLE && COMPRESS && !DEFRAG_ALWAYS && MV_STORE && !AUTOCOMMIT)"]}]