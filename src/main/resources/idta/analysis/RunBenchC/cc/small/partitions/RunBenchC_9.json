[{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.command.ddl","className":"CreateIndex","methodSignature":"update()I","decisionIndex":4,"info":["(IFEXISTS && !FORBID_CREATION)","(!IFEXISTS && FORBID_CREATION)","(IFEXISTS && FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"append(Ljava/lang/Object;Ljava/lang/Object;)V","decisionIndex":2,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":33,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readIfMore()Z","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTableEngine$Store","methodSignature":"close(J)V","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"findOldChunks(I)Ljava/lang/Iterable;","decisionIndex":4,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactMoveChunks(Ljava/lang/Iterable;)V","decisionIndex":14,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"Page","methodSignature":"insert([JIIJ)[J","decisionIndex":2,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"createIdSet(Ljava/lang/Iterable;)Ljava/util/HashSet;","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Command","methodSignature":"filterConcurrentUpdate(Lorg/h2/message/DbException;J)J","decisionIndex":3,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"openDatabase(IIZLorg/h2/engine/ConnectionInfo;)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":34,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"append(Ljava/lang/Object;Ljava/lang/Object;)V","decisionIndex":3,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTableEngine$Store","methodSignature":"close(J)V","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":6,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"read(Ljava/nio/ByteBuffer;I)V","decisionIndex":8,"info":["(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.value","className":"DataType","methodSignature":"isDateTimeType(I)Z","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"findOldChunks(I)Ljava/lang/Iterable;","decisionIndex":5,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":10,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"IndexCondition","methodSignature":"isAlwaysFalse()Z","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.cache","className":"CacheLongKeyLIRS$Segment","methodSignature":"find(JI)Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"getTargetFillRate()I","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":35,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"addTableToDependencies(Lorg/h2/mvstore/db/MVTable;Ljava/util/HashSet;)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && !AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!IFEXISTS && FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseDelete()Lorg/h2/command/dml/Delete;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore.db","className":"MVTableEngine$Store","methodSignature":"close(J)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseInsertGivenTable(Lorg/h2/command/dml/Insert;Lorg/h2/table/Table;)Lorg/h2/command/dml/Insert;","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"updateRootPage(Lorg/h2/mvstore/Page;J)Lorg/h2/mvstore/RootReference;","decisionIndex":2,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":9,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"getAllowLiterals()I","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAndGetRoot()Lorg/h2/mvstore/RootReference;","decisionIndex":2,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseColumnWithType(Ljava/lang/String;Z)Lorg/h2/table/Column;","decisionIndex":10,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.index","className":"IndexCursor","methodSignature":"prepare(Lorg/h2/engine/Session;Ljava/util/ArrayList;)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseDelete()Lorg/h2/command/dml/Delete;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"incrementChangeCount()V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":36,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.index","className":"IndexCursor","methodSignature":"next()Z","decisionIndex":1,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseInsertGivenTable(Lorg/h2/command/dml/Insert;Lorg/h2/table/Table;)Lorg/h2/command/dml/Insert;","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"updateRootPage(Lorg/h2/mvstore/Page;J)Lorg/h2/mvstore/RootReference;","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ColumnNamer","methodSignature":"getColumnName(Lorg/h2/expression/Expression;ILjava/lang/String;)Ljava/lang/String;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseColumnWithType(Ljava/lang/String;Z)Lorg/h2/table/Column;","decisionIndex":11,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"ConditionAndOr","methodSignature":"createIndexConditions(Lorg/h2/engine/Session;Lorg/h2/table/TableFilter;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression","className":"ExpressionVisitor","methodSignature":"allColumnsForTableFilters([Lorg/h2/table/TableFilter;Lorg/h2/command/dml/AllColumnsForPlan;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseColumnWithType(Ljava/lang/String;Z)Lorg/h2/table/Column;","decisionIndex":16,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"setWriteVersion(J)Lorg/h2/mvstore/RootReference;","decisionIndex":4,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.expression.function","className":"Function","methodSignature":"getSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;","decisionIndex":6,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"checkCanceled()V","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"queryWithoutCacheLazyCheck(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":4,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.result","className":"RowImpl","methodSignature":"getValue(I)Lorg/h2/value/Value;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":37,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseInsertGivenTable(Lorg/h2/command/dml/Insert;Lorg/h2/table/Table;)Lorg/h2/command/dml/Insert;","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"validateConvertUpdateSequence(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":3,"info":["(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType$LongType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V","decisionIndex":6,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"isEverything(Lorg/h2/expression/ExpressionVisitor;)Z","decisionIndex":9,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"getValue(Lorg/h2/table/Column;)Lorg/h2/value/Value;","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactMoveChunks(Ljava/lang/Iterable;)V","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType$LongType","methodSignature":"write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V","decisionIndex":5,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readTableFilter()Lorg/h2/table/TableFilter;","decisionIndex":19,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":38,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"queryWithoutCacheLazyCheck(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":3,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseInsertGivenTable(Lorg/h2/command/dml/Insert;Lorg/h2/table/Table;)Lorg/h2/command/dml/Insert;","decisionIndex":6,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionMap$CommittedIterator","methodSignature":"fetchNext()V","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)","(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)","(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)","(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"setWriteVersion(J)Lorg/h2/mvstore/RootReference;","decisionIndex":5,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.pagestore","className":"PageStreamTrunk","methodSignature":"contains(I)Z","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.jdbc","className":"JdbcConnection","methodSignature":"translateSQL(Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readAnd(Lorg/h2/expression/Expression;)Lorg/h2/expression/Expression;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseColumnWithType(Ljava/lang/String;Z)Lorg/h2/table/Column;","decisionIndex":9,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":31,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.expression.function","className":"Function","methodSignature":"getSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;","decisionIndex":4,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"read(Ljava/lang/String;)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":39,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.store","className":"FileLock","methodSignature":"load()Ljava/util/Properties;","decisionIndex":2,"info":["(IFEXISTS || FORBID_CREATION || !FILE_LOCK)","(!IFEXISTS && !FORBID_CREATION && FILE_LOCK)"]},{"packageName":"org.h2.store","className":"FileStore","methodSignature":"setLength(J)V","decisionIndex":2,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactMoveChunks(Ljava/lang/Iterable;)V","decisionIndex":12,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"RowImpl","methodSignature":"setValue(ILorg/h2/value/Value;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"<init>(Lorg/h2/mvstore/RootReference;I)V","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":3,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"sameResultAsLast(Lorg/h2/engine/Session;[Lorg/h2/value/Value;[Lorg/h2/value/Value;J)Z","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"markAsRemoved()Z","decisionIndex":6,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeCursor","methodSignature":"next()Z","decisionIndex":2,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.expression.function","className":"Function","methodSignature":"getSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;","decisionIndex":5,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseSet()Lorg/h2/command/Prepared;","decisionIndex":32,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"read(Ljava/lang/String;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactMoveChunks(Ljava/lang/Iterable;)V","decisionIndex":11,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"edu.cmu.cs.mvelezce.bench.c","className":"BenchCThread","methodSignature":"processNewOrder()V","decisionIndex":11,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Command","methodSignature":"filterConcurrentUpdate(Lorg/h2/message/DbException;J)J","decisionIndex":2,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeLeaf","methodSignature":"memoryChange()V","decisionIndex":2,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)","(FORBID_CREATION && !PAGE_SIZE && IFEXISTS && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !PAGE_SIZE && IFEXISTS && !MV_STORE)","(FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && !MV_STORE)","(FORBID_CREATION && !PAGE_SIZE && IFEXISTS && MV_STORE)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && !MV_STORE)","(!FORBID_CREATION && !PAGE_SIZE && IFEXISTS && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"CacheLRU","methodSignature":"get(I)Lorg/h2/util/CacheObject;","decisionIndex":1,"info":["(IFEXISTS && FORBID_CREATION && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(IFEXISTS && !FORBID_CREATION && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(IFEXISTS && FORBID_CREATION && MV_STORE)","(!IFEXISTS && FORBID_CREATION && MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"setPageSize(I)V","decisionIndex":5,"info":["(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)","(IFEXISTS || FORBID_CREATION || MV_STORE)"]}]