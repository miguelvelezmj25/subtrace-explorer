[{"packageName":"org.h2.command","className":"CommandContainer","methodSignature":"update(Ljava/lang/Object;)Lorg/h2/result/ResultWithGeneratedKeys;","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Page$Leaf","methodSignature":"setValue(ILjava/lang/Object;)Ljava/lang/Object;","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"findInsertionPoint(J)I","decisionIndex":1,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreateTable(ZZZ)Lorg/h2/command/ddl/CreateTable;","decisionIndex":20,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"removeTemporaryLobs(Z)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION))","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)","(AUTOCOMMIT && (IFEXISTS || FORBID_CREATION))"]},{"packageName":"org.h2.engine","className":"Engine","methodSignature":"openSession(Lorg/h2/engine/ConnectionInfo;ZZLjava/lang/String;)Lorg/h2/engine/Session;","decisionIndex":5,"info":["IFEXISTS","(!IFEXISTS && FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"<init>(Lorg/h2/mvstore/RootReference;JI)V","decisionIndex":4,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"isOpenOrStopping()Z","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Engine","methodSignature":"openSession(Lorg/h2/engine/ConnectionInfo;ZZLjava/lang/String;)Lorg/h2/engine/Session;","decisionIndex":8,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":19,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"tryCommit()J","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageLog","methodSignature":"logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V","decisionIndex":1,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":26,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"readValue()Lorg/h2/value/Value;","decisionIndex":23,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Update","methodSignature":"update()I","decisionIndex":24,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"findInsertionPoint(J)I","decisionIndex":2,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreateTable(ZZZ)Lorg/h2/command/ddl/CreateTable;","decisionIndex":19,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":27,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.util","className":"Utils","methodSignature":"newIntArray(I)[I","decisionIndex":1,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"FileLock","methodSignature":"lock(Lorg/h2/store/FileLockMethod;)V","decisionIndex":2,"info":["(IFEXISTS || FORBID_CREATION || !FILE_LOCK)","(!IFEXISTS && !FORBID_CREATION && FILE_LOCK)"]},{"packageName":"org.h2.value","className":"ValueInt","methodSignature":"get(I)Lorg/h2/value/ValueInt;","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactRewrite(Ljava/util/Set;)I","decisionIndex":7,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"getVarIntLen(I)I","decisionIndex":2,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE))","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":25,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"addRowTry(Lorg/h2/result/Row;)I","decisionIndex":1,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":26,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactMoveChunks(Ljava/lang/Iterable;)V","decisionIndex":21,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"getOldestVersionToKeep()J","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"setSortSetting(IZZ)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"tryLock(I)Lorg/h2/mvstore/RootReference;","decisionIndex":3,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"expandColumnList()V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"asString()Ljava/lang/String;","decisionIndex":5,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.value","className":"ValueStringFixed","methodSignature":"get(Ljava/lang/String;)Lorg/h2/value/ValueStringFixed;","decisionIndex":2,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"addIndex(Lorg/h2/engine/Session;Ljava/lang/String;I[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;ZLjava/lang/String;)Lorg/h2/index/Index;","decisionIndex":3,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"initialize(Ljava/lang/String;)V","decisionIndex":33,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":21,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.pagestore","className":"PageLog","methodSignature":"logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V","decisionIndex":3,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"getCostRangeIndex([IJ[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;ZLorg/h2/command/dml/AllColumnsForPlan;)J","decisionIndex":11,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getConstraintType()Lorg/h2/constraint/Constraint$Type;","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"appendMap(Ljava/lang/StringBuilder;Ljava/util/HashMap;)Ljava/lang/StringBuilder;","decisionIndex":2,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":35,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"insertKey(ILjava/lang/Object;)V","decisionIndex":3,"info":["(IFEXISTS && FORBID_CREATION && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && !MV_STORE)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && MV_STORE)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && MV_STORE)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && MV_STORE)","(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":29,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"RootReference","methodSignature":"tryLock(I)Lorg/h2/mvstore/RootReference;","decisionIndex":2,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"LocalResultImpl","methodSignature":"cloneLobs([Lorg/h2/value/Value;)V","decisionIndex":2,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"expandColumnList()V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtree","methodSignature":"getRow(I)Lorg/h2/result/SearchRow;","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.pagestore.db","className":"PageDelegateIndex","methodSignature":"<init>(Lorg/h2/pagestore/db/PageStoreTable;ILjava/lang/String;Lorg/h2/index/IndexType;Lorg/h2/pagestore/db/PageDataIndex;ZLorg/h2/engine/Session;)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":23,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"Value","methodSignature":"convertTo(ILorg/h2/value/ExtTypeInfo;Lorg/h2/engine/CastDataProvider;ZLjava/lang/Object;)Lorg/h2/value/Value;","decisionIndex":2,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"split(I)Lorg/h2/pagestore/db/PageData;","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.index","className":"BaseIndex","methodSignature":"getCostRangeIndex([IJ[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;ZLorg/h2/command/dml/AllColumnsForPlan;)J","decisionIndex":12,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"<init>(Lorg/h2/command/ddl/CreateTableData;)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"doLock2(Lorg/h2/engine/Session;IZ)Z","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"parseUInt31(Ljava/lang/String;II)I","decisionIndex":7,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(IFEXISTS || FORBID_CREATION || !MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"insertKey(ILjava/lang/Object;)V","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"rebuildIndex(Lorg/h2/engine/Session;Lorg/h2/mvstore/db/MVIndex;Ljava/lang/String;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(IFEXISTS || FORBID_CREATION || !MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"accountForRemovedPage(JJZ)V","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"split(I)Lorg/h2/pagestore/db/PageData;","decisionIndex":2,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"readValue()Lorg/h2/value/Value;","decisionIndex":27,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readSimpleTableFilter(ILjava/util/Collection;)Lorg/h2/table/TableFilter;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"compact(I)V","decisionIndex":13,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"write(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)I","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.table","className":"Column","methodSignature":"validateConvertUpdateSequence(Lorg/h2/engine/Session;Lorg/h2/value/Value;)Lorg/h2/value/Value;","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageStore","methodSignature":"writePage(ILorg/h2/store/Data;)V","decisionIndex":5,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"readFully(Ljava/nio/channels/FileChannel;JLjava/nio/ByteBuffer;)V","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readTableFilter()Lorg/h2/table/TableFilter;","decisionIndex":11,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"parseUInt31(Ljava/lang/String;II)I","decisionIndex":8,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(IFEXISTS || FORBID_CREATION || !MV_STORE)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":44,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"FileStore","methodSignature":"write([BII)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(FORBID_CREATION && !PAGE_SIZE && IFEXISTS && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !PAGE_SIZE && IFEXISTS && !MV_STORE)","(FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && !MV_STORE)","(FORBID_CREATION && !PAGE_SIZE && IFEXISTS && MV_STORE)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && !MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !PAGE_SIZE && IFEXISTS && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Command","methodSignature":"stop()V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":21,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"MVTable","methodSignature":"addIndex(Lorg/h2/engine/Session;Ljava/lang/String;I[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;ZLjava/lang/String;)Lorg/h2/index/Index;","decisionIndex":4,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"check()V","decisionIndex":3,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":7,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageLog","methodSignature":"logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V","decisionIndex":4,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"appendMap(Ljava/lang/StringBuilder;Ljava/util/HashMap;)Ljava/lang/StringBuilder;","decisionIndex":3,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readSimpleTableFilter(ILjava/util/Collection;)Lorg/h2/table/TableFilter;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"commit(Z)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && !AUTOCOMMIT)","(!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION))","(!IFEXISTS && !FORBID_CREATION && AUTOCOMMIT)","(AUTOCOMMIT && (IFEXISTS || FORBID_CREATION))"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"deleteOldTempFiles()V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && !FILE_LOCK)","(!IFEXISTS && !FORBID_CREATION && FILE_LOCK)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.command.ddl","className":"AlterTableAlterColumn","methodSignature":"checkClustering(Lorg/h2/table/Column;)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Page$NonLeaf","methodSignature":"writeEnd()V","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.value","className":"ValueCollectionBase","methodSignature":"getMemory()I","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.expression.function","className":"Function","methodSignature":"mapColumns(Lorg/h2/table/ColumnResolver;II)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore.tx","className":"CommitDecisionMaker","methodSignature":"decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;","decisionIndex":2,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"log(Lorg/h2/table/Table;SLorg/h2/result/Row;)V","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS && !FORBID_CREATION)","(!IFEXISTS && FORBID_CREATION)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.engine","className":"Engine","methodSignature":"openSession(Lorg/h2/engine/ConnectionInfo;ZZLjava/lang/String;)Lorg/h2/engine/Session;","decisionIndex":6,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"tryCommit()J","decisionIndex":3,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":28,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"getVarIntLen(I)I","decisionIndex":3,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE))","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeLeaf","methodSignature":"memoryChange()V","decisionIndex":1,"info":["(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)"]},{"packageName":"org.h2.util","className":"MathUtils","methodSignature":"convertLongToInt(J)I","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":4,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore","className":"PageFreeList","methodSignature":"write()V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeNode","methodSignature":"updateRowCount(I)V","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"compactRewrite(Ljava/util/Set;)I","decisionIndex":5,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"readValue()Lorg/h2/value/Value;","decisionIndex":26,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"DataUtils","methodSignature":"getVarIntLen(I)I","decisionIndex":4,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE))","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"]}]