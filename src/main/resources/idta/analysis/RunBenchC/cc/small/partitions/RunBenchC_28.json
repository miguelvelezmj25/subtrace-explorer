[{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.mvstore","className":"Page$PageReference","methodSignature":"clearPageReference()V","decisionIndex":7,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"scrubMetaMap()V","decisionIndex":5,"info":["(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":2,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueString","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":2,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":13,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"clearIt()Lorg/h2/mvstore/RootReference;","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE || !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"parseUInt31(Ljava/lang/String;II)I","decisionIndex":6,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueString","methodSignature":"equals(Ljava/lang/Object;)Z","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":8,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageBtreeIndex","methodSignature":"remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":2,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":6,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":12,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.jdbc","className":"JdbcPreparedStatement","methodSignature":"setString(ILjava/lang/String;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"queryWithoutCache(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":22,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.jdbc","className":"JdbcResultSet","methodSignature":"checkOnValidRow()V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"]},{"packageName":"org.h2.store","className":"Data","methodSignature":"writeVarInt(I)V","decisionIndex":1,"info":["(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"endTransaction(Lorg/h2/mvstore/tx/Transaction;Z)V","decisionIndex":10,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.value","className":"ValueStringFixed","methodSignature":"get(Ljava/lang/String;)Lorg/h2/value/ValueStringFixed;","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.result","className":"LocalResultImpl","methodSignature":"<init>(Lorg/h2/engine/Session;[Lorg/h2/expression/Expression;II)V","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"quoteIdentifier(Ljava/lang/StringBuilder;Ljava/lang/String;Z)Ljava/lang/StringBuilder;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"isConditionMet()Z","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"parseUInt31(Ljava/lang/String;II)I","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore.type","className":"StringDataType","methodSignature":"read(Ljava/nio/ByteBuffer;[Ljava/lang/Object;IZ)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore","className":"MVMap","methodSignature":"clearIt()Lorg/h2/mvstore/RootReference;","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE || !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"log(Lorg/h2/table/Table;SLorg/h2/result/Row;)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":12,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":5,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"removeUnusableIndexConditions()V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.util","className":"StringUtils","methodSignature":"parseUInt31(Ljava/lang/String;II)I","decisionIndex":9,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(IFEXISTS || FORBID_CREATION || !MV_STORE)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":11,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"write(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)I","decisionIndex":3,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"freeFileSpace(JI)V","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"writeChunkHeader(Lorg/h2/mvstore/WriteBuffer;I)V","decisionIndex":3,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":13,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":4,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore$BackgroundWriterThread","methodSignature":"run()V","decisionIndex":1,"info":["(!IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)","(IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)","(IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","(IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)"]},{"packageName":"org.h2.table","className":"TableFilter","methodSignature":"removeUnusableIndexConditions()V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":10,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.mvstore.tx","className":"CommitDecisionMaker","methodSignature":"selectValue(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/value/VersionedValue;","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.db","className":"ValueDataType","methodSignature":"getMemory(Ljava/lang/Object;)I","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"writeChunkHeader(Lorg/h2/mvstore/WriteBuffer;I)V","decisionIndex":2,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command.dml","className":"SelectGroups","methodSignature":"setCurrentGroupExprData(Lorg/h2/expression/Expression;Ljava/lang/Object;)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":14,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"scrubMetaMap()V","decisionIndex":9,"info":["(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"addUndoLogRecord(IJ[Ljava/lang/Object;)J","decisionIndex":1,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataIndex","methodSignature":"add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V","decisionIndex":1,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION)","(IFEXISTS && FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(IFEXISTS && !FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Update","methodSignature":"update()I","decisionIndex":2,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)","(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)","(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"]},{"packageName":"org.h2.engine","className":"Database","methodSignature":"addMeta(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"getBestPlanItem(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":3,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.index","className":"MetaIndex","methodSignature":"getCost(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)D","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":6,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Chunk","methodSignature":"writeChunkHeader(Lorg/h2/mvstore/WriteBuffer;I)V","decisionIndex":1,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readFactor()Lorg/h2/expression/Expression;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":15,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"storeNow(JJ)V","decisionIndex":8,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":7,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"scrubMetaMap()V","decisionIndex":2,"info":["(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"getBestPlanItem(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"TransactionStore","methodSignature":"addUndoLogRecord(IJ[Ljava/lang/Object;)J","decisionIndex":2,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.constraint","className":"ConstraintUnique","methodSignature":"getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;","decisionIndex":2,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command.dml","className":"Query","methodSignature":"sameResultAsLast(Lorg/h2/engine/Session;[Lorg/h2/value/Value;[Lorg/h2/value/Value;J)Z","decisionIndex":9,"info":["(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.tx","className":"CommitDecisionMaker","methodSignature":"selectValue(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/value/VersionedValue;","decisionIndex":3,"info":["(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"readFactor()Lorg/h2/expression/Expression;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"setAutoCommitDelay(I)V","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":16,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.message","className":"Trace","methodSignature":"error(Ljava/lang/Throwable;Ljava/lang/String;)V","decisionIndex":1,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":22,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"write(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)I","decisionIndex":2,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parsePrepared()Lorg/h2/command/Prepared;","decisionIndex":13,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)","(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"Page$PageReference","methodSignature":"clearPageReference()V","decisionIndex":5,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"queryWithoutCache(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":21,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore","className":"MVStore","methodSignature":"scrubMetaMap()V","decisionIndex":3,"info":["(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)"]},{"packageName":"org.h2.pagestore.db","className":"PageDataLeaf","methodSignature":"writeData()V","decisionIndex":3,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION || MV_STORE)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":12,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.table","className":"Table","methodSignature":"getBestPlanItem(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;","decisionIndex":3,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.engine","className":"Engine","methodSignature":"openSession(Lorg/h2/engine/ConnectionInfo;ZZLjava/lang/String;)Lorg/h2/engine/Session;","decisionIndex":14,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":4,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseQuerySub()Lorg/h2/command/dml/Query;","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseCreate()Lorg/h2/command/Prepared;","decisionIndex":17,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.util","className":"ParserUtil","methodSignature":"getSaveTokenType(Ljava/lang/String;ZIIZ)I","decisionIndex":23,"info":["(IFEXISTS || FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION)"]},{"packageName":"org.h2.expression","className":"BinaryOperation","methodSignature":"getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;","decisionIndex":1,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.mvstore.tx","className":"Transaction","methodSignature":"isActive(I)Z","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Select","methodSignature":"queryWithoutCache(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;","decisionIndex":20,"info":["(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)"]},{"packageName":"org.h2.engine","className":"Session","methodSignature":"startStatementWithinTransaction(Lorg/h2/command/Command;)V","decisionIndex":2,"info":["(!IFEXISTS && !FORBID_CREATION && !AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION)","(!IFEXISTS && FORBID_CREATION)","(!IFEXISTS && !FORBID_CREATION && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION)"]},{"packageName":"org.h2.expression.condition","className":"Comparison","methodSignature":"optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;","decisionIndex":13,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page$PageReference","methodSignature":"clearPageReference()V","decisionIndex":4,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.pagestore","className":"Page","methodSignature":"insert([Ljava/lang/Object;IILjava/lang/Object;)[Ljava/lang/Object;","decisionIndex":2,"info":["(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)","(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"]},{"packageName":"org.h2.command.dml","className":"Insert","methodSignature":"prepare()V","decisionIndex":5,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.pagestore.db","className":"PageStoreTable","methodSignature":"traceLock(Lorg/h2/engine/Session;ZLjava/lang/String;)V","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)","(IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && FORBID_CREATION && !MV_STORE && AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)","(IFEXISTS && FORBID_CREATION && MV_STORE && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore","className":"Page","methodSignature":"write(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)I","decisionIndex":8,"info":["(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)","(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)","(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)","(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)","((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))"]},{"packageName":"org.h2.command","className":"Parser","methodSignature":"parseQuerySub()Lorg/h2/command/dml/Query;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)","(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)","(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)","(IFEXISTS || FORBID_CREATION)","(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"]},{"packageName":"org.h2.mvstore.type","className":"ObjectDataType","methodSignature":"newType(I)Lorg/h2/mvstore/type/ObjectDataType$AutoDetectDataType;","decisionIndex":1,"info":["(!IFEXISTS && !FORBID_CREATION && MV_STORE)","(IFEXISTS || FORBID_CREATION || !MV_STORE)"]}]