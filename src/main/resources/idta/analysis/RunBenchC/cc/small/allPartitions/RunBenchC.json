[
  "(CACHE_SIZE || !AUTOCOMMIT)",
  "(!CACHE_SIZE && !FILE_LOCK)",
  "(CACHE_SIZE && AUTOCOMMIT)",
  "(!FILE_LOCK && !ACCESS_MODE_DATA)",
  "(AUTOCOMMIT && FILE_LOCK && !CACHE_SIZE)",
  "(!AUTOCOMMIT && FILE_LOCK && CACHE_SIZE)",
  "(!CACHE_SIZE && FILE_LOCK)",
  "(!AUTOCOMMIT && FILE_LOCK && !CACHE_SIZE)",
  "CACHE_SIZE",
  "!FILE_LOCK",
  "(CACHE_SIZE && !AUTOCOMMIT)",
  "(!CACHE_SIZE && !AUTOCOMMIT)",
  "(AUTOCOMMIT && !FILE_LOCK && !CACHE_SIZE)",
  "!AUTOCOMMIT",
  "(!AUTOCOMMIT && !FILE_LOCK && !CACHE_SIZE)",
  "CACHE_TYPE",
  "(!CACHE_SIZE || !AUTOCOMMIT)",
  "(!CACHE_SIZE && AUTOCOMMIT)",
  "(FILE_LOCK && ACCESS_MODE_DATA)",
  "FILE_LOCK",
  "(AUTOCOMMIT && FILE_LOCK && CACHE_SIZE)",
  "(!FILE_LOCK && ACCESS_MODE_DATA)",
  "(FILE_LOCK && !ACCESS_MODE_DATA)",
  "AUTOCOMMIT",
  "!CACHE_TYPE",
  "(AUTOCOMMIT && !FILE_LOCK && CACHE_SIZE)",
  "!CACHE_SIZE",
  "(!AUTOCOMMIT && !FILE_LOCK && CACHE_SIZE)"
]