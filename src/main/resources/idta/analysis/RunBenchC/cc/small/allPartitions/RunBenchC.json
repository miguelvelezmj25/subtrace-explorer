[
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
  "(AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)) && FILE_LOCK)",
  "((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION)))",
  "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS)",
  "(!IFEXISTS && FORBID_CREATION && MV_STORE && CACHE_SIZE)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS)",
  "(!IFEXISTS && FORBID_CREATION && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "((CACHE_SIZE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!CACHE_SIZE && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)))",
  "(IFEXISTS || FORBID_CREATION || FILE_LOCK)",
  "(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)))",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)",
  "((AUTOCOMMIT && ((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))) || (!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS)",
  "(!IFEXISTS && FORBID_CREATION && !MV_STORE)",
  "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE || FORBID_CREATION || IFEXISTS || MV_STORE || !FILE_LOCK)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK))",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK))",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE && CACHE_SIZE)",
  "(!IFEXISTS && !FORBID_CREATION && !FILE_LOCK)",
  "(IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "((FILE_LOCK && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)) || (!FILE_LOCK && (FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK))",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE)",
  "(!AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)))",
  "((!FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !CACHE_SIZE))",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && ANALYZE_AUTO)",
  "(!MV_STORE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
  "((AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || !MV_STORE)) || (!AUTOCOMMIT && (FORBID_CREATION || !COMPRESS || IFEXISTS || !MV_STORE)))",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
  "(!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)))",
  "(!FORBID_CREATION && !PAGE_SIZE && IFEXISTS && !MV_STORE)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT) || (AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)) && !FILE_LOCK))",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)))",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK))",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && FILE_LOCK)",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE && FILE_LOCK)",
  "(IFEXISTS && !FORBID_CREATION && MV_STORE && !CACHE_SIZE)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE)",
  "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
  "((!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS) || (!FILE_LOCK && ((!FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !CACHE_SIZE))))",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)",
  "(!IFEXISTS && !FORBID_CREATION && ANALYZE_AUTO && !MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !ANALYZE_AUTO && !MV_STORE)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)",
  "(FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
  "((FILE_LOCK && ((AUTOCOMMIT && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)) || (!AUTOCOMMIT && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || IFEXISTS || MV_STORE)))) || (!FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || IFEXISTS || MV_STORE)))",
  "((FILE_LOCK && ((AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && ((!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && DEFRAG_ALWAYS) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS)) && !CACHE_SIZE))) || (!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE))))) || (!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE))))",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && ANALYZE_AUTO)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION)",
  "((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && MV_STORE && !CACHE_SIZE)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE))",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
  "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)",
  "((FILE_LOCK && ((AUTOCOMMIT && ((CACHE_SIZE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))) || (!AUTOCOMMIT && ((CACHE_SIZE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))))) || (!FILE_LOCK && ((CACHE_SIZE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))))",
  "((!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK))",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "((FILE_LOCK && ((AUTOCOMMIT && (!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)) || (!AUTOCOMMIT && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))) || (!FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))",
  "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT) || (!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)))))",
  "((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS)",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE)",
  "((CACHE_SIZE && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE)))",
  "(FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
  "((!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK))",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
  "(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && ANALYZE_AUTO && !AUTOCOMMIT)",
  "(!FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT))",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && ANALYZE_AUTO)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)))",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE && !CACHE_SIZE)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)",
  "(AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)) && !FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && MV_STORE && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
  "(!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || CACHE_SIZE || IFEXISTS || MV_STORE || !FILE_LOCK || !AUTOCOMMIT)",
  "(!PAGE_SIZE || FORBID_CREATION || IFEXISTS || MV_STORE || !FILE_LOCK || !AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "((FILE_LOCK && ((AUTOCOMMIT && ((CACHE_SIZE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)) || (!CACHE_SIZE && (!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))) || (!AUTOCOMMIT && (FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))) || (!FILE_LOCK && (FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT)))",
  "(AUTOCOMMIT && ((!FORBID_CREATION && !COMPRESS && !IFEXISTS && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE)))",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)) && !FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
  "AUTOCOMMIT",
  "(AUTOCOMMIT && (IFEXISTS || FORBID_CREATION))",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && ANALYZE_AUTO)",
  "((CACHE_SIZE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))",
  "(!IFEXISTS && !FORBID_CREATION && !ANALYZE_AUTO && MV_STORE)",
  "(!IFEXISTS && FORBID_CREATION && !MV_STORE && !CACHE_SIZE)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)) && FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && !PAGE_SIZE && IFEXISTS && !MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && !FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)) && !FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION || MV_STORE)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT)",
  "(!FILE_LOCK && ((AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS))) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)))",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(IFEXISTS || !FORBID_CREATION)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT))) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS))",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS))",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK))",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "((FILE_LOCK && ((AUTOCOMMIT && ((!FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS) || (!CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE) || (!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS))))) || (!AUTOCOMMIT && ((!FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !CACHE_SIZE))))) || (!FILE_LOCK && ((!FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !CACHE_SIZE))))",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK))",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT))) || (!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK))",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT))) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK))",
  "(FORBID_CREATION && !PAGE_SIZE && IFEXISTS && MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS)",
  "(!MV_STORE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && FILE_LOCK)",
  "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "((!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT) || (!AUTOCOMMIT && ((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE))))",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE)",
  "!IFEXISTS",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT))) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS))",
  "(!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)) && !FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE && FILE_LOCK)",
  "((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK) || (!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE))))",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK))",
  "(!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION))",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)))",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !MV_STORE && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE)",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE && CACHE_SIZE)",
  "(!FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS)",
  "(!IFEXISTS && !FORBID_CREATION && !ANALYZE_AUTO && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && ANALYZE_AUTO)",
  "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "((FILE_LOCK && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!FILE_LOCK && ((CACHE_SIZE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!CACHE_SIZE && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)))))",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS))",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)",
  "((!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK))",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS)",
  "(FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT))) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK))",
  "(AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)))",
  "(IFEXISTS || FORBID_CREATION || !MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
  "(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)))",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)) && !FILE_LOCK))",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE)",
  "!AUTOCOMMIT",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "((AUTOCOMMIT && ((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE))) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !AUTOCOMMIT))",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS)",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && !FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && !MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
  "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && ANALYZE_AUTO)",
  "((FILE_LOCK && ((AUTOCOMMIT && ((CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))) || (!CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!MV_STORE && ((!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && DEFRAG_ALWAYS) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS))))))) || (!AUTOCOMMIT && ((CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))) || (!IFEXISTS && !FORBID_CREATION && !CACHE_SIZE))))) || (!FILE_LOCK && ((CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))) || (!IFEXISTS && !FORBID_CREATION && !CACHE_SIZE))))",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS))",
  "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "((FILE_LOCK && ((CACHE_SIZE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!CACHE_SIZE && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)))) || (!FILE_LOCK && (FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE)",
  "(IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)",
  "(IFEXISTS && FORBID_CREATION && MV_STORE && !AUTOCOMMIT)",
  "((AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || !MV_STORE)) || (!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || !MV_STORE)))",
  "(IFEXISTS && FORBID_CREATION && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK) || (!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS))))",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)",
  "(FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK) || (!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK))",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS))",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "((AUTOCOMMIT && ((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))) || (!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))",
  "(FILE_LOCK && ((AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS))) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)))",
  "(!IFEXISTS && !FORBID_CREATION && ANALYZE_AUTO)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(FILE_LOCK && ((!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)))",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
  "(!FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK)",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS))",
  "(IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION || ANALYZE_AUTO)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(!IFEXISTS && FORBID_CREATION && !MV_STORE && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "((MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION)))",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(IFEXISTS && FORBID_CREATION && MV_STORE && AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK))",
  "((FILE_LOCK && ((AUTOCOMMIT && ((CACHE_SIZE && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))) || (!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || MV_STORE || CACHE_SIZE)))) || (!FILE_LOCK && (IFEXISTS || FORBID_CREATION || MV_STORE || CACHE_SIZE)))",
  "(AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)) && FILE_LOCK)",
  "((FILE_LOCK && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))) || (!FILE_LOCK && ((CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))) || (!IFEXISTS && !FORBID_CREATION && !CACHE_SIZE))))",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT) || (AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)) && !FILE_LOCK))",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)",
  "(!IFEXISTS && FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE)",
  "(!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)))",
  "(IFEXISTS || FORBID_CREATION || !FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS)",
  "((FILE_LOCK && ((AUTOCOMMIT && (!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)) || (!AUTOCOMMIT && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))) || (!FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE || !AUTOCOMMIT)))",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(IFEXISTS && !FORBID_CREATION && MV_STORE && CACHE_SIZE)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && ANALYZE_AUTO)",
  "((!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT) || (!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && !AUTOCOMMIT))",
  "(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)",
  "(IFEXISTS && FORBID_CREATION && MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
  "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)))",
  "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && !MV_STORE)",
  "((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK))",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
  "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && !MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && !ANALYZE_AUTO)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !ANALYZE_AUTO)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !ANALYZE_AUTO && !AUTOCOMMIT)",
  "((FILE_LOCK && (IFEXISTS || FORBID_CREATION)) || (!FILE_LOCK && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))",
  "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
  "(FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && ANALYZE_AUTO)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !MV_STORE && CACHE_SIZE)",
  "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE)",
  "((FILE_LOCK && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS || MV_STORE)) || (!FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || IFEXISTS || MV_STORE)))",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "((FILE_LOCK && (IFEXISTS || FORBID_CREATION || MV_STORE)) || (!FILE_LOCK && ((CACHE_SIZE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !PAGE_SIZE || IFEXISTS)) || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))))",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE && !FILE_LOCK)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK) || (!FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS))))",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT))",
  "(IFEXISTS && FORBID_CREATION && DEFRAG_ALWAYS && !MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !COMPRESS && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE)",
  "((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)) || !MV_STORE || !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && !MV_STORE)",
  "(!IFEXISTS && !FORBID_CREATION && FILE_LOCK)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && MV_STORE)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS)",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE && !FILE_LOCK)",
  "((AUTOCOMMIT && (IFEXISTS || FORBID_CREATION)) || (!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || MV_STORE)))",
  "((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT) || (!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT))",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
  "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(FORBID_CREATION || !COMPRESS || IFEXISTS || !MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
  "(!FORBID_CREATION && !PAGE_SIZE && IFEXISTS && MV_STORE)",
  "(!IFEXISTS && !FORBID_CREATION && MV_STORE && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)) && FILE_LOCK)",
  "(!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS)",
  "((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)) && !FILE_LOCK))",
  "(IFEXISTS && !FORBID_CREATION && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !MV_STORE && !CACHE_SIZE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && !FILE_LOCK)",
  "(IFEXISTS && !FORBID_CREATION && !MV_STORE && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE || FORBID_CREATION || IFEXISTS || !COMPRESS || !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
  "(IFEXISTS && FORBID_CREATION && MV_STORE && !CACHE_SIZE)",
  "(!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)))",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && !PAGE_SIZE && !IFEXISTS && MV_STORE)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK))",
  "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS))) || (!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE))))",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
  "((FILE_LOCK && (IFEXISTS || FORBID_CREATION || MV_STORE)) || (!FILE_LOCK && ((CACHE_SIZE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)) || (!CACHE_SIZE && (IFEXISTS || FORBID_CREATION || MV_STORE)))))",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(IFEXISTS && !FORBID_CREATION)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
  "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && ANALYZE_AUTO && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE || !FILE_LOCK || !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !MV_STORE)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS)",
  "(FILE_LOCK && ((!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)))",
  "(IFEXISTS && !FORBID_CREATION && !ANALYZE_AUTO)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
  "(!IFEXISTS && FORBID_CREATION && MV_STORE && !AUTOCOMMIT)",
  "(!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || IFEXISTS || MV_STORE || !FILE_LOCK || !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
  "((FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT) || (!AUTOCOMMIT && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS))))) || (!FILE_LOCK && ((!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS))))",
  "((!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT) || (!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT))",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "((!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !CACHE_SIZE))",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
  "(!IFEXISTS && FORBID_CREATION)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !MV_STORE)",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !FILE_LOCK))",
  "IFEXISTS",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && AUTOCOMMIT)",
  "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "((AUTOCOMMIT && ((MV_STORE && (IFEXISTS || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))) || (!AUTOCOMMIT && ((MV_STORE && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)))) || (!MV_STORE && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS)))))",
  "(FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)))",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK)",
  "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && FILE_LOCK)",
  "(!IFEXISTS && FORBID_CREATION && !MV_STORE && CACHE_SIZE)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "((CACHE_SIZE && ((!IFEXISTS && !FORBID_CREATION && MV_STORE) || (!IFEXISTS && !FORBID_CREATION && !DEFRAG_ALWAYS && !MV_STORE))) || (!IFEXISTS && !FORBID_CREATION && !CACHE_SIZE))",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && AUTOCOMMIT)",
  "((AUTOCOMMIT && ((DEFRAG_ALWAYS && (IFEXISTS || FORBID_CREATION)) || (!DEFRAG_ALWAYS && (FORBID_CREATION || !COMPRESS || IFEXISTS)) || !MV_STORE)) || (!AUTOCOMMIT && (IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || !MV_STORE)))",
  "(IFEXISTS && !FORBID_CREATION && !AUTOCOMMIT)",
  "((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS))",
  "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && !MV_STORE && CACHE_SIZE)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
  "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && AUTOCOMMIT)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !AUTOCOMMIT))) || (!IFEXISTS && !FORBID_CREATION && !MV_STORE && !FILE_LOCK))",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
  "((!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS))",
  "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION || !DEFRAG_ALWAYS || MV_STORE)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
  "(IFEXISTS && FORBID_CREATION && !MV_STORE)",
  "(!AUTOCOMMIT && ((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE) || (!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)) && FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
  "(IFEXISTS && FORBID_CREATION && MV_STORE && CACHE_SIZE)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION || !MV_STORE || !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
  "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
  "(!IFEXISTS && !FORBID_CREATION && !ANALYZE_AUTO)",
  "(!MV_STORE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(IFEXISTS || FORBID_CREATION || MV_STORE || CACHE_SIZE)",
  "((FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE || !AUTOCOMMIT)) || (!FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE || !AUTOCOMMIT)))",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)",
  "(IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT)",
  "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
  "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
  "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
  "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && DEFRAG_ALWAYS)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && !FILE_LOCK)",
  "(!IFEXISTS && FORBID_CREATION && FILE_LOCK)",
  "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
  "(IFEXISTS && !FORBID_CREATION && FILE_LOCK)",
  "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && ANALYZE_AUTO && AUTOCOMMIT)",
  "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
  "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
  "((FILE_LOCK && ((!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT))) || (!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK))",
  "((!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT) || (!IFEXISTS && !FORBID_CREATION && MV_STORE && !AUTOCOMMIT))",
  "((!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT) || (!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT))",
  "((FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)) || (!FILE_LOCK && (!PAGE_SIZE || FORBID_CREATION || !CACHE_SIZE || !DEFRAG_ALWAYS || IFEXISTS || MV_STORE)))",
  "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
  "(!IFEXISTS && !FORBID_CREATION && DEFRAG_ALWAYS && FILE_LOCK)",
  "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
  "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)"
]