	(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && !USE_COMPOUND_FILE && !COMMIT_ON_CLOSE) -> (MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE)
	(!MAX_BUFFERED_DOCS && USE_COMPOUND_FILE && COMMIT_ON_CLOSE && !MAX_TOKEN_LENGTH) -> (!MAX_TOKEN_LENGTH && COMMIT_ON_CLOSE)
	(!COMMIT_ON_CLOSE && !MAX_BUFFERED_DOCS && !MAX_TOKEN_LENGTH) -> (!MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE)
	(!MAX_BUFFERED_DOCS && MAX_TOKEN_LENGTH && RAM_BUFFER_SIZE_MB && USE_COMPOUND_FILE && !COMMIT_ON_CLOSE) -> (MAX_TOKEN_LENGTH && !COMMIT_ON_CLOSE)
